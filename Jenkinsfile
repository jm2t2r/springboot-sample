echo "---build start---"

stage('Checkout Stage') {
    echo "---Checkout---"
    ls -al
}

stage('Build Stage') {
    echo "---Build Stage---"
    cd ~
    docker version
}

stage('Push Stage') {
    echo "---Push Stage---"
    java -version
}
