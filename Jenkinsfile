echo "---build start---"

stage('Checkout Stage') {
    echo "---Checkout---"
    sh "ls -al"
}

stage('Build Stage') {
    echo "---Build Stage---"
    sh "cd ~"
    sh "docker version"
}

stage('Push Stage') {
    echo "---Push Stage---"
    sh "java -version"
}
